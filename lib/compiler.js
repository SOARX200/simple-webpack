const path = requre('path');
const fs = require('fs');

module.exports = class Compiler {
  constructor({entry, output}) {
    this.entry = entry;
    this.output = output;
    this.modules = [];
  }

  run() {

  }

  buildModule() {

  }

  emitFiles() {
    
  }
};